import { BaseClass } from "../utils";

/**
 * Base class for all events in Beynac.
 * Events are objects that represent something that happened in the application.
 */
export abstract class BeynacEvent extends BaseClass {}
