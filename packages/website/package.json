{
  "name": "@beynac/website",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev",
    "build": "bun run generate-progress && bun run build:api && bun run build:vitepress",
    "build:api": "typedoc",
    "build:vitepress": "vitepress build",
    "preview": "vitepress preview",
    "generate-progress": "bun scripts/generate-paths.ts && bun scripts/generate-progress.ts"
  },
  "devDependencies": {
    "beynac": "workspace:",
    "typedoc": "^0.28.9",
    "vitepress": "^1.5.0",
    "vue": "^3.5.13"
  }
}
